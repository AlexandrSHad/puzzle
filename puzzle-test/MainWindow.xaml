<Window x:Class="puzzle_test.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:puzzle_test"
        mc:Ignorable="d"
        Title="MainWindow" Height="525" Width="525"
        
        MouseDown="Window_MouseDown"
        
        AllowDrop="True"
        Drop="Window_Drop"
        DragOver="Window_DragOver">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ItemsControl Name="itemsControl" ItemsSource="{Binding}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid x:Name="gridBoard" ShowGridLines="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition />
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <!--<ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                            <TextBlock Text="{Binding currentRow}" Background="Aqua" />
                            <TextBlock Text="{Binding currentColumn}" Background="Azure" />
                            <TextBlock Text="{Binding currentPosition}" Background="Yellow" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>-->
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type local:Tag}">
                    <Image Source="{Binding ImagePath}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="{x:Type FrameworkElement}">
                    <Setter Property="Grid.Row" Value="{Binding CurrentRow}" />
                    <Setter Property="Grid.Column" Value="{Binding CurrentColumn}" />
                    <!--<Setter Property="AllowDrop" Value="True" />
                    <EventSetter Event="Drop" Handler="ItemControlBoard_Drop" />-->
                    <EventSetter Event="PreviewMouseMove" Handler="ItemControlBoard_PreviewMouseMove" />
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>

        <Button Name="buttonMove" Content="Move" Grid.Row="1" Click="buttonMove_Click" />
    </Grid>
</Window>
